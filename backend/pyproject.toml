[tool.pytest.ini_options]
pythonpath = ["."]
markers = [
  "integration: requires DB_PASSWORD and read-only access to Azure SQL demo",
]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F"]
ignore = ["E402", "E501"]
